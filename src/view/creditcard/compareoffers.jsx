import React from 'react';
import Popup from 'react-popup';
 
const Compareoffers = ({offerid}) => (
    <div style={{backgroundColor:'#FFE8CE'}}>
  ReactDom.render(
        <Popup />,
        document.getElementById('popupContainer')

    </div>



);

export default Compareoffers;